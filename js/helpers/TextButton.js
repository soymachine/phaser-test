class TextButton
{
    constructor ({text, game, x = 0, y = 0, spriteName = 'bgButton'})
    {
        /*
        console.log(game)
        this.button = game.add.button(x, y, spriteName);
        this.button.anchor.setTo(0.5);
        
        this.text = game.add.text(this.button.x, this.button.y, text, fontStyle);
        this.text.anchor.setTo(0.5); 
        */

    }


    
}

export default TextButton;